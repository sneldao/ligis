{
  "name": "@ligis/contracts-casper",
  "version": "0.1.0",
  "description": "Casper (Odra/Rust) contracts for Ligis — agent identity & capability credentials",
  "private": true,
  "keywords": [
    "casper",
    "odra",
    "rust",
    "agent",
    "identity",
    "credentials",
    "eip-712"
  ],
  "scripts": {
    "build": "PATH=\"$HOME/.cargo/bin:$HOME/.rustup/toolchains/nightly-2026-01-01-aarch64-apple-darwin/bin:$PATH\" cargo odra build",
    "test": "PATH=\"$HOME/.cargo/bin:$HOME/.rustup/toolchains/nightly-2026-01-01-aarch64-apple-darwin/bin:$PATH\" cargo odra test",
    "test:casper": "PATH=\"$HOME/.cargo/bin:$HOME/.rustup/toolchains/nightly-2026-01-01-aarch64-apple-darwin/bin:$PATH\" cargo odra test -b casper",
    "clean": "cargo clean"
  }
}
